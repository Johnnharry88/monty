#include "monty.h"
/**
 * nop_stack_int - function that does nothing to dlist
 * @head: pointer to the first node of the dlist(stack).
 * @c_line: line number.
 * Return: Void.
 */
void nop_stack_int(stack_t **head, unsigned int c_line)
{
	(void)head;
	(void)c_line;
}
